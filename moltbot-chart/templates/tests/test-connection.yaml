apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moltbot-chart.fullname" . }}-test-connection"
  labels:
    {{- include "moltbot-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "moltbot-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
